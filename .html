<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lucky Ahlawat — Portfolio</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Inter:wght@300;400;500;600;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }

    :root {
      --bg: #0A0A0A;
      --surface: #111111;
      --surface-raised: #181818;
      --border: #222222;
      --gold: #C9A55A;
      --gold-light: #D4B878;
      --gold-dim: rgba(201,165,90,0.10);
      --blue: #5B8DB8;
      --blue-dim: rgba(91,141,184,0.10);
      --text: #F0ECE4;
      --text-muted: #7A7570;
      --text-faint: #363330;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Inter', sans-serif;
      font-size: 15px;
      line-height: 1.65;
      overflow-x: hidden;
    }

    /* ─── NAV ─── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; justify-content: space-between; align-items: center;
      padding: 22px 56px;
      background: rgba(10,10,10,0.85);
      backdrop-filter: blur(16px);
      border-bottom: 1px solid transparent;
      transition: border-color 0.4s;
    }
    nav.scrolled { border-bottom-color: var(--border); }
    .nav-logo {
      font-family: 'Space Mono', monospace;
      font-size: 13px; color: var(--gold);
      letter-spacing: 0.12em;
    }
    .nav-links { display: flex; gap: 36px; list-style: none; }
    .nav-links a {
      font-family: 'Space Mono', monospace;
      font-size: 11px; color: var(--text-muted);
      text-decoration: none;
      letter-spacing: 0.1em; text-transform: uppercase;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--gold); }

    /* ─── HERO ─── */
    .hero {
      min-height: 100vh;
      display: grid; grid-template-columns: 1.1fr 0.9fr;
      align-items: center; padding: 0 56px;
      position: relative; overflow: hidden; gap: 40px;
    }

    /* Blueprint grid bg */
    .hero-bg-grid {
      position: absolute; inset: 0; z-index: 0;
      background-image:
        linear-gradient(rgba(201,165,90,0.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(201,165,90,0.035) 1px, transparent 1px);
      background-size: 48px 48px;
      pointer-events: none;
      mask-image: radial-gradient(ellipse 80% 80% at 70% 50%, black 20%, transparent 75%);
    }

    /* Gear SVG rotating in bg */
    .hero-gear {
      position: absolute; right: 8%; top: 50%;
      transform: translateY(-50%);
      z-index: 0; pointer-events: none;
      width: 420px; height: 420px;
      opacity: 0.035;
    }
    .hero-gear .gear-outer { animation: rotateSlow 90s linear infinite; transform-origin: 50% 50%; }
    .hero-gear .gear-inner { animation: rotateSlowReverse 60s linear infinite; transform-origin: 50% 50%; }

    .hero-content { position: relative; z-index: 1; padding-top: 80px; }

    .hero-eyebrow {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--gold); letter-spacing: 0.3em; text-transform: uppercase;
      margin-bottom: 24px;
      opacity: 0; transform: translateY(18px);
      animation: fadeUp 0.6s 0.2s ease forwards;
    }

    .hero-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(54px, 5.8vw, 88px);
      font-weight: 300; line-height: 1.0;
      color: var(--text); letter-spacing: -0.025em;
      opacity: 0; transform: translateY(28px);
      animation: fadeUp 0.8s 0.35s ease forwards;
    }
    .hero-name em {
      color: var(--gold); font-style: italic; font-weight: 400;
    }

    .hero-desc {
      margin-top: 28px; font-size: 14px; color: var(--text-muted);
      line-height: 1.75; max-width: 440px;
      opacity: 0; transform: translateY(18px);
      animation: fadeUp 0.7s 0.6s ease forwards;
    }

    .hero-pills {
      display: flex; flex-wrap: wrap; gap: 8px; margin-top: 28px;
      opacity: 0; transform: translateY(18px);
      animation: fadeUp 0.7s 0.8s ease forwards;
    }
    .hero-pill {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--text-muted); border: 1px solid var(--border);
      padding: 5px 13px; border-radius: 2px; letter-spacing: 0.06em;
    }

    .hero-cta {
      display: flex; gap: 14px; margin-top: 36px;
      opacity: 0; transform: translateY(18px);
      animation: fadeUp 0.7s 1.0s ease forwards;
    }
    .btn-gold {
      font-family: 'Space Mono', monospace; font-size: 11px;
      letter-spacing: 0.12em; text-transform: uppercase;
      color: #0A0A0A; background: var(--gold);
      padding: 13px 26px; text-decoration: none; border-radius: 2px;
      transition: opacity 0.2s, transform 0.2s;
    }
    .btn-gold:hover { opacity: 0.82; transform: translateY(-2px); }
    .btn-outline {
      font-family: 'Space Mono', monospace; font-size: 11px;
      letter-spacing: 0.12em; text-transform: uppercase;
      color: var(--text-muted); border: 1px solid var(--border);
      padding: 13px 26px; text-decoration: none; border-radius: 2px;
      transition: color 0.2s, border-color 0.2s, transform 0.2s;
    }
    .btn-outline:hover { color: var(--text); border-color: #3A3A3A; transform: translateY(-2px); }

    .hero-stats {
      display: flex; gap: 44px; margin-top: 48px;
      padding-top: 36px; border-top: 1px solid var(--border);
      opacity: 0; transform: translateY(18px);
      animation: fadeUp 0.7s 1.2s ease forwards;
    }
    .stat-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 38px; font-weight: 500;
      color: var(--gold); line-height: 1;
    }
    .stat-lbl {
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--text-muted); letter-spacing: 0.12em;
      text-transform: uppercase; margin-top: 5px;
    }

    /* Hero Photo */
    .hero-photo-side {
      position: relative; z-index: 1;
      display: flex; justify-content: center; align-items: center;
      padding-top: 80px;
      opacity: 0; transform: translateX(32px);
      animation: fadeLeft 0.9s 0.5s ease forwards;
    }
    .photo-wrap {
      position: relative; width: 340px; height: 460px;
    }
    .photo-wrap::before {
      content: ''; position: absolute; inset: 0;
      border: 1px solid rgba(201,165,90,0.35);
      border-radius: 2px; z-index: 1; pointer-events: none;
    }
    .photo-wrap::after {
      content: ''; position: absolute;
      top: 14px; left: 14px; right: -14px; bottom: -14px;
      border: 1px solid var(--border);
      border-radius: 2px; z-index: -1;
    }
    .photo-wrap img {
      width: 100%; height: 100%;
      object-fit: cover; object-position: top center;
      border-radius: 2px; display: block;
      filter: brightness(0.97) contrast(1.03);
    }
    .photo-caption {
      position: absolute; bottom: -32px; left: 0;
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--text-faint); letter-spacing: 0.2em;
      text-transform: uppercase;
    }
    /* Corner accents */
    .corner {
      position: absolute; width: 16px; height: 16px;
      border-color: var(--gold); border-style: solid; z-index: 2;
      opacity: 0.6;
    }
    .corner.tl { top: -4px; left: -4px; border-width: 1px 0 0 1px; }
    .corner.tr { top: -4px; right: -4px; border-width: 1px 1px 0 0; }
    .corner.bl { bottom: -4px; left: -4px; border-width: 0 0 1px 1px; }
    .corner.br { bottom: -4px; right: -4px; border-width: 0 1px 1px 0; }

    /* ─── SECTIONS ─── */
    section { padding: 96px 56px; }
    section + section { border-top: 1px solid var(--border); }

    .sec-header { margin-bottom: 56px; }
    .sec-eyebrow {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--gold); letter-spacing: 0.28em;
      text-transform: uppercase; margin-bottom: 12px;
    }
    .sec-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(36px, 4vw, 54px);
      font-weight: 300; color: var(--text);
      line-height: 1.05; letter-spacing: -0.02em;
    }
    .sec-line {
      width: 48px; height: 1px;
      background: var(--gold); margin-top: 20px; opacity: 0.5;
    }

    /* ─── PROJECTS ─── */
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
      gap: 2px; background: var(--border);
    }
    .proj-card {
      background: var(--surface); padding: 36px 38px;
      position: relative; overflow: hidden;
      transition: background 0.25s;
    }
    .proj-card::before {
      content: ''; position: absolute;
      left: 0; top: 0; bottom: 0; width: 3px;
      background: linear-gradient(180deg, var(--gold), transparent);
      transform: scaleY(0); transform-origin: top;
      transition: transform 0.35s ease;
    }
    .proj-card:hover { background: var(--surface-raised); }
    .proj-card:hover::before { transform: scaleY(1); }

    .proj-year {
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--text-faint); letter-spacing: 0.18em;
      text-transform: uppercase; margin-bottom: 10px;
    }
    .proj-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 22px; font-weight: 500;
      color: var(--text); line-height: 1.2; margin-bottom: 12px;
    }
    .proj-tags {
      display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 22px;
    }
    .proj-tag {
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--blue); background: var(--blue-dim);
      padding: 3px 9px; border-radius: 2px; letter-spacing: 0.06em;
    }
    .proj-bullets { list-style: none; display: flex; flex-direction: column; gap: 9px; }
    .proj-bullets li {
      font-size: 12.5px; color: var(--text-muted);
      line-height: 1.6; padding-left: 14px; position: relative;
    }
    .proj-bullets li::before {
      content: '—'; position: absolute; left: 0;
      color: var(--gold); font-size: 11px; top: 1px;
    }
    .proj-bullets .num { color: var(--gold-light); font-weight: 600; }

    /* ─── EVENTS ─── */
    .timeline { position: relative; padding-left: 28px; }
    .timeline::before {
      content: ''; position: absolute; left: 0; top: 8px; bottom: 0;
      width: 1px; background: linear-gradient(var(--border), transparent);
    }
    .tl-item { position: relative; padding: 0 0 52px 44px; }
    .tl-dot {
      position: absolute; left: -5px; top: 7px;
      width: 10px; height: 10px; border-radius: 50%;
      background: var(--gold); box-shadow: 0 0 8px rgba(201,165,90,0.5);
    }
    .tl-meta {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--gold); letter-spacing: 0.18em;
      text-transform: uppercase; margin-bottom: 8px;
    }
    .tl-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 28px; font-weight: 500;
      color: var(--text); margin-bottom: 6px; line-height: 1.15;
    }
    .tl-subtitle {
      font-size: 12px; color: var(--text-muted); margin-bottom: 18px;
    }
    .tl-list { list-style: none; display: flex; flex-direction: column; gap: 10px; max-width: 680px; }
    .tl-list li {
      font-size: 12.5px; color: var(--text-muted); line-height: 1.6;
      padding-left: 14px; position: relative;
    }
    .tl-list li::before {
      content: '–'; position: absolute; left: 0;
      color: var(--gold); font-size: 12px;
    }
    .tl-list .num { color: var(--gold-light); font-weight: 600; }

    /* ─── SKILLS ─── */
    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 2px; background: var(--border);
    }
    .skill-cat {
      background: var(--surface); padding: 32px 36px;
      transition: background 0.2s;
    }
    .skill-cat:hover { background: var(--surface-raised); }
    .skill-cat-lbl {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--gold); letter-spacing: 0.2em;
      text-transform: uppercase; margin-bottom: 18px;
    }
    .chips { display: flex; flex-wrap: wrap; gap: 7px; }
    .chip {
      font-size: 12px; color: var(--text-muted);
      border: 1px solid var(--border);
      padding: 5px 12px; border-radius: 2px;
      transition: color 0.2s, border-color 0.2s;
      cursor: default;
    }
    .chip:hover { color: var(--text); border-color: #333; }

    /* ─── SPORTS ─── */
    .sports-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 2px; background: var(--border);
    }
    .sport-card {
      background: var(--surface); padding: 40px 36px;
      text-align: center; transition: background 0.2s;
      position: relative; overflow: hidden;
    }
    .sport-card:hover { background: var(--surface-raised); }
    .sport-icon { font-size: 32px; margin-bottom: 16px; display: block; }
    .sport-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 22px; font-weight: 500; color: var(--text);
      margin-bottom: 6px;
    }
    .sport-badge {
      display: inline-block;
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--gold); letter-spacing: 0.15em;
      text-transform: uppercase;
      border: 1px solid rgba(201,165,90,0.3);
      padding: 3px 10px; border-radius: 2px;
    }

    .gym-card {
      background: var(--surface); padding: 40px 36px;
      grid-column: span 3;
      display: flex; align-items: center; gap: 36px;
      transition: background 0.2s;
    }
    .gym-card:hover { background: var(--surface-raised); }
    .gym-icon { font-size: 36px; flex-shrink: 0; }
    .gym-content { flex: 1; }
    .gym-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 22px; font-weight: 500; color: var(--text);
      margin-bottom: 6px;
    }
    .gym-desc { font-size: 12.5px; color: var(--text-muted); line-height: 1.6; }

    /* ─── EDUCATION ─── */
    .edu-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 2px; background: var(--border);
    }
    .edu-card {
      background: var(--surface); padding: 34px 36px;
      transition: background 0.2s;
    }
    .edu-card:hover { background: var(--surface-raised); }
    .edu-year {
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--text-faint); letter-spacing: 0.18em;
      text-transform: uppercase; margin-bottom: 12px;
    }
    .edu-school {
      font-family: 'Cormorant Garamond', serif;
      font-size: 20px; font-weight: 500; color: var(--text);
      margin-bottom: 6px; line-height: 1.3;
    }
    .edu-degree { font-size: 12px; color: var(--text-muted); line-height: 1.6; }
    .edu-badge {
      display: inline-block; margin-top: 14px;
      font-family: 'Space Mono', monospace; font-size: 9px;
      color: var(--gold); background: var(--gold-dim);
      padding: 4px 10px; border-radius: 2px;
      letter-spacing: 0.1em; text-transform: uppercase;
    }

    /* ─── FOOTER ─── */
    footer {
      padding: 52px 56px;
      border-top: 1px solid var(--border);
      display: flex; justify-content: space-between; align-items: center;
    }
    .footer-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 30px; font-weight: 300;
      color: var(--text-faint);
    }
    .footer-links { display: flex; gap: 28px; }
    .footer-links a {
      font-family: 'Space Mono', monospace; font-size: 10px;
      color: var(--text-muted); text-decoration: none;
      letter-spacing: 0.1em; text-transform: uppercase;
      transition: color 0.2s;
    }
    .footer-links a:hover { color: var(--gold); }

    /* ─── REVEAL ─── */
    .reveal {
      opacity: 0; transform: translateY(22px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .reveal.visible { opacity: 1; transform: none; }

    /* ─── ANIMATIONS ─── */
    @keyframes fadeUp {
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeLeft {
      to { opacity: 1; transform: translateX(0); }
    }
    @keyframes rotateSlow {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    @keyframes rotateSlowReverse {
      from { transform: rotate(0deg); }
      to { transform: rotate(-360deg); }
    }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 860px) {
      nav { padding: 16px 24px; }
      .nav-links { display: none; }
      .hero {
        grid-template-columns: 1fr; padding: 100px 24px 60px;
        text-align: center; gap: 48px;
      }
      .hero-photo-side { order: -1; padding-top: 0; }
      .photo-wrap { width: 220px; height: 300px; }
      .hero-pills, .hero-cta, .hero-stats { justify-content: center; }
      .hero-desc { margin: 0 auto; }
      section { padding: 70px 24px; }
      .projects-grid { grid-template-columns: 1fr; }
      .sports-grid { grid-template-columns: 1fr; }
      .gym-card { grid-column: span 1; }
      footer { flex-direction: column; gap: 20px; text-align: center; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
      }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav id="navbar">
  <div class="nav-logo">LA_</div>
  <ul class="nav-links">
    <li><a href="#projects">Projects</a></li>
    <li><a href="#events">Events</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#sports">Sports</a></li>
    <li><a href="#education">Education</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero" id="hero">
  <div class="hero-bg-grid"></div>
  <svg class="hero-gear" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" fill="none">
    <g class="gear-outer" stroke="white" stroke-width="0.8">
      <circle cx="100" cy="100" r="88"/>
      <circle cx="100" cy="100" r="72"/>
      <rect x="96" y="8" width="8" height="16" rx="1"/>
      <rect x="96" y="176" width="8" height="16" rx="1"/>
      <rect x="8" y="96" width="16" height="8" rx="1"/>
      <rect x="176" y="96" width="16" height="8" rx="1"/>
      <rect x="128" y="12" width="8" height="14" rx="1" transform="rotate(22.5 132 19)"/>
      <rect x="60" y="12" width="8" height="14" rx="1" transform="rotate(-22.5 64 19)"/>
      <rect x="128" y="174" width="8" height="14" rx="1" transform="rotate(-22.5 132 181)"/>
      <rect x="60" y="174" width="8" height="14" rx="1" transform="rotate(22.5 64 181)"/>
      <rect x="12" y="128" width="14" height="8" rx="1" transform="rotate(-22.5 19 132)"/>
      <rect x="174" y="128" width="14" height="8" rx="1" transform="rotate(22.5 181 132)"/>
      <rect x="12" y="64" width="14" height="8" rx="1" transform="rotate(22.5 19 68)"/>
      <rect x="174" y="64" width="14" height="8" rx="1" transform="rotate(-22.5 181 68)"/>
      <line x1="100" y1="0" x2="100" y2="200"/>
      <line x1="0" y1="100" x2="200" y2="100"/>
      <line x1="29" y1="29" x2="171" y2="171"/>
      <line x1="171" y1="29" x2="29" y2="171"/>
    </g>
    <g class="gear-inner" stroke="white" stroke-width="0.6">
      <circle cx="100" cy="100" r="48"/>
      <circle cx="100" cy="100" r="16"/>
      <line x1="100" y1="52" x2="100" y2="68"/>
      <line x1="100" y1="132" x2="100" y2="148"/>
      <line x1="52" y1="100" x2="68" y2="100"/>
      <line x1="132" y1="100" x2="148" y2="100"/>
      <line x1="66" y1="66" x2="78" y2="78"/>
      <line x1="134" y1="66" x2="122" y2="78"/>
      <line x1="66" y1="134" x2="78" y2="122"/>
      <line x1="134" y1="134" x2="122" y2="122"/>
    </g>
  </svg>

  <div class="hero-content">
    <div class="hero-eyebrow">B.Tech Mechanical Engineering &nbsp;·&nbsp; DTU '29</div>
    <h1 class="hero-name">Lucky<br><em>Ahlawat</em></h1>
    <p class="hero-desc">
      First-year engineer at Delhi Technological University, building at the intersection of AI,
      embedded systems, and mobile development. Five projects shipped. Thousands of lives touched.
    </p>
    <div class="hero-pills">
      <span class="hero-pill">AI &amp; ML</span>
      <span class="hero-pill">Android Dev</span>
      <span class="hero-pill">IoT</span>
      <span class="hero-pill">Embedded Systems</span>
      <span class="hero-pill">Public Relations</span>
    </div>
    <div class="hero-cta">
      <a href="mailto:luckyahlawat2007@gmail.com" class="btn-gold">Get in Touch</a>
      <a href="#projects" class="btn-outline">View Projects</a>
    </div>
    <div class="hero-stats">
      <div>
        <div class="stat-num" data-target="5">0</div>
        <div class="stat-lbl">Projects Shipped</div>
      </div>
      <div>
        <div class="stat-num" data-target="5">0</div>
        <div class="stat-lbl">Events Managed</div>
      </div>
      <div>
        <div class="stat-num" data-target="10">0</div>
        <div class="stat-lbl">Thousand+ Reached</div>
      </div>
    </div>
  </div>

  <div class="hero-photo-side">
    <div class="photo-wrap">
      <div class="corner tl"></div>
      <div class="corner tr"></div>
      <div class="corner bl"></div>
      <div class="corner br"></div>
      <!-- Replace with: data:image/jpeg;base64,YOUR_BASE64_STRING -->
      <!-- OR use: src="photo.jpeg" if the image is in the same folder -->
      <img src="YOUR_PHOTO_BASE64_HERE" alt="Lucky Ahlawat">
      <div class="photo-caption">New Delhi, India &nbsp;·&nbsp; 2025</div>
    </div>
  </div>
</section>

<!-- PROJECTS -->
<section id="projects">
  <div class="sec-header reveal">
    <div class="sec-eyebrow">01 &nbsp;—&nbsp; Engineering</div>
    <h2 class="sec-title">Selected Projects</h2>
    <div class="sec-line"></div>
  </div>
  <div class="projects-grid">

    <div class="proj-card reveal">
      <div class="proj-year">2025</div>
      <div class="proj-name">Dost AI</div>
      <div class="proj-tags">
        <span class="proj-tag">AI</span>
        <span class="proj-tag">Android</span>
        <span class="proj-tag">Voice UI</span>
      </div>
      <ul class="proj-bullets">
        <li>AI-powered assistive Android app enabling <span class="num">100+</span> visually impaired users to identify real-time surroundings with <span class="num">95%+</span> object-detection accuracy.</li>
        <li>Fully voice-driven pipeline reducing hands-free interaction friction by <span class="num">80%</span> vs screen-reader alternatives.</li>
        <li>Achieved WCAG 2.1 AA compliance, cutting onboarding time by <span class="num">60%</span>.</li>
      </ul>
    </div>

    <div class="proj-card reveal">
      <div class="proj-year">2025</div>
      <div class="proj-name">AI Vehicle Lock System</div>
      <div class="proj-tags">
        <span class="proj-tag">AI</span>
        <span class="proj-tag">Embedded Systems</span>
        <span class="proj-tag">Security</span>
      </div>
      <ul class="proj-bullets">
        <li>Biometric access control delivering <span class="num">99.7%</span> authentication accuracy, reducing unauthorised access by <span class="num">85%</span>.</li>
        <li>Hardware-software co-design pipeline cutting response latency by <span class="num">40%</span>.</li>
        <li>Firmware deployed across <span class="num">3</span> independent security components.</li>
      </ul>
    </div>

    <div class="proj-card reveal">
      <div class="proj-year">2025</div>
      <div class="proj-name">Attendance Monitoring System</div>
      <div class="proj-tags">
        <span class="proj-tag">Android</span>
        <span class="proj-tag">SQLite</span>
        <span class="proj-tag">Automation</span>
      </div>
      <ul class="proj-bullets">
        <li>Digital platform managing <span class="num">500+</span> student records, eliminating manual entry and cutting data errors by <span class="num">90%</span>.</li>
        <li>Automated report generation improving admin speed by <span class="num">70%</span>, recovering <span class="num">5+</span> staff-hours per week.</li>
      </ul>
    </div>

    <div class="proj-card reveal">
      <div class="proj-year">2025</div>
      <div class="proj-name">Tuition Centre Management</div>
      <div class="proj-tags">
        <span class="proj-tag">Kotlin</span>
        <span class="proj-tag">Firebase</span>
        <span class="proj-tag">EdTech</span>
      </div>
      <ul class="proj-bullets">
        <li>Kotlin-Firebase app managing records for <span class="num">50+</span> students across <span class="num">3</span> coaching centres.</li>
        <li>Reduced daily admin overhead by <span class="num">65%</span> — from 3 hours to under 1 hour per day.</li>
      </ul>
    </div>

    <div class="proj-card reveal">
      <div class="proj-year">2025</div>
      <div class="proj-name">IoT Medicine Reminder</div>
      <div class="proj-tags">
        <span class="proj-tag">Arduino</span>
        <span class="proj-tag">Sensors</span>
        <span class="proj-tag">HealthTech</span>
      </div>
      <ul class="proj-bullets">
        <li>Arduino-powered device integrating <span class="num">3</span> sensor modules with <span class="num">98%</span> alert reliability.</li>
        <li>Designed for <span class="num">30+</span> elderly users, reducing missed-dose incidents by <span class="num">75%</span>.</li>
      </ul>
    </div>

  </div>
</section>

<!-- EVENTS -->
<section id="events">
  <div class="sec-header reveal">
    <div class="sec-eyebrow">02 &nbsp;—&nbsp; Experience</div>
    <h2 class="sec-title">Events &amp; Public Relations</h2>
    <div class="sec-line"></div>
  </div>
  <div class="timeline">
    <div class="tl-item reveal">
      <div class="tl-dot"></div>
      <div class="tl-meta">2023 — 2025</div>
      <div class="tl-title">Public Relations Member</div>
      <div class="tl-subtitle">Large-Scale Event Management &nbsp;·&nbsp; New Delhi, India</div>
      <ul class="tl-list">
        <li>Orchestrated PR campaigns for <span class="num">5</span> high-profile events — Comicorn, Corporate Promotion Drive, Anime India, Grub Fest, and Engi Fest DTU — reaching <span class="num">10,000+</span> attendees.</li>
        <li>Amplified event footfall by <span class="num">30%</span> through social-media outreach, influencer coordination, and on-ground activation.</li>
        <li>Coordinated with <span class="num">15+</span> vendors, sponsors, and college committees across events of 500 to 5,000 participants.</li>
        <li>Generated <span class="num">25,000+</span> combined impressions, contributing to sold-out registrations for <span class="num">2</span> consecutive flagship events.</li>
      </ul>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section id="skills">
  <div class="sec-header reveal">
    <div class="sec-eyebrow">03 &nbsp;—&nbsp; Capabilities</div>
    <h2 class="sec-title">Technical Skills</h2>
    <div class="sec-line"></div>
  </div>
  <div class="skills-grid">
    <div class="skill-cat reveal">
      <div class="skill-cat-lbl">Languages</div>
      <div class="chips">
        <span class="chip">Kotlin</span>
        <span class="chip">Python</span>
        <span class="chip">C</span>
        <span class="chip">C++</span>
      </div>
    </div>
    <div class="skill-cat reveal">
      <div class="skill-cat-lbl">Frameworks &amp; Platforms</div>
      <div class="chips">
        <span class="chip">Android Studio</span>
        <span class="chip">Firebase</span>
        <span class="chip">Arduino IDE</span>
        <span class="chip">SQLite</span>
        <span class="chip">Git</span>
      </div>
    </div>
    <div class="skill-cat reveal">
      <div class="skill-cat-lbl">Technical Domains</div>
      <div class="chips">
        <span class="chip">Artificial Intelligence</span>
        <span class="chip">IoT</span>
        <span class="chip">Embedded Systems</span>
        <span class="chip">Mobile Dev</span>
        <span class="chip">HealthTech</span>
        <span class="chip">EdTech</span>
      </div>
    </div>
    <div class="skill-cat reveal">
      <div class="skill-cat-lbl">Professional Skills</div>
      <div class="chips">
        <span class="chip">Public Relations</span>
        <span class="chip">Event Management</span>
        <span class="chip">Stakeholder Comms</span>
        <span class="chip">Team Coordination</span>
      </div>
    </div>
  </div>
</section>

<!-- SPORTS -->
<section id="sports">
  <div class="sec-header reveal">
    <div class="sec-eyebrow">04 &nbsp;—&nbsp; Athletics</div>
    <h2 class="sec-title">Sports &amp; Fitness</h2>
    <div class="sec-line"></div>
  </div>
  <div class="sports-grid">
    <div class="sport-card reveal">
      <span class="sport-icon">🏏</span>
      <div class="sport-name">Cricket</div>
      <span class="sport-badge">Zone Level</span>
    </div>
    <div class="sport-card reveal">
      <span class="sport-icon">🤼</span>
      <div class="sport-name">Kabaddi</div>
      <span class="sport-badge">Zone Level</span>
    </div>
    <div class="sport-card reveal">
      <span class="sport-icon">🏋️</span>
      <div class="sport-name">Wrestling</div>
      <span class="sport-badge">Zone Level</span>
    </div>
    <div class="gym-card reveal">
      <span class="gym-icon">💪</span>
      <div class="gym-content">
        <div class="gym-name">Strength &amp; Conditioning</div>
        <div class="gym-desc">
          Consistent strength-training regimen focused on weightlifting and progressive overload —
          building resilience, discipline, and endurance directly transferable to high-pressure technical environments.
        </div>
      </div>
    </div>
  </div>
</section>

<!-- EDUCATION -->
<section id="education">
  <div class="sec-header reveal">
    <div class="sec-eyebrow">05 &nbsp;—&nbsp; Academics</div>
    <h2 class="sec-title">Education</h2>
    <div class="sec-line"></div>
  </div>
  <div class="edu-grid">
    <div class="edu-card reveal">
      <div class="edu-year">2025 — 2029</div>
      <div class="edu-school">Delhi Technological University</div>
      <div class="edu-degree">Bachelor of Technology<br>Mechanical Engineering</div>
      <span class="edu-badge">Current</span>
    </div>
    <div class="edu-card reveal">
      <div class="edu-year">Completed 2024</div>
      <div class="edu-school">GBSS Prashant Vihar</div>
      <div class="edu-degree">Senior Secondary Certificate (Class XII)<br>Science Stream</div>
    </div>
    <div class="edu-card reveal">
      <div class="edu-year">Completed 2022</div>
      <div class="edu-school">GBSS Sector-11, Rohini</div>
      <div class="edu-degree">Secondary School Certificate<br>Class X</div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-name">Lucky Ahlawat</div>
  <div class="footer-links">
    <a href="mailto:luckyahlawat2007@gmail.com">Email</a>
    <a href="https://linkedin.com/in/luckyahlawat" target="_blank">LinkedIn</a>
    <a href="tel:+918700716883">+91 87007 16883</a>
  </div>
</footer>

<script>
  // Nav border on scroll
  const nav = document.getElementById('navbar');
  window.addEventListener('scroll', () => {
    nav.classList.toggle('scrolled', window.scrollY > 60);
  });

  // Scroll reveal
  const reveals = document.querySelectorAll('.reveal');
  const io = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 80);
        io.unobserve(e.target);
      }
    });
  }, { threshold: 0.1 });
  reveals.forEach(el => io.observe(el));

  // Count-up animation
  const counters = document.querySelectorAll('.stat-num[data-target]');
  const counterIO = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const el = entry.target;
        const target = +el.dataset.target;
        let current = 0;
        const step = Math.ceil(target / 30);
        const timer = setInterval(() => {
          current = Math.min(current + step, target);
          el.textContent = current + (target >= 10 ? 'K+' : '+');
          if (current >= target) clearInterval(timer);
        }, 50);
        counterIO.unobserve(el);
      }
    });
  }, { threshold: 0.5 });
  counters.forEach(el => counterIO.observe(el));
</script>

</body>
</html>